const errorHandler = require('./error');
const isArrayNumbers = require('./isArrayNumbers');
const isNumber = require('./isNumber');

module.exports = {
    errorHandler,
    isArrayNumbers,
    isNumber
};